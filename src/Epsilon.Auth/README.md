# Epsilon.Auth

This module owns:

- account authentication
- SSO and session tickets
- bans and access checks
- session audit metadata
- password cryptography and verification primitives

It should not depend on client-version packet details.
