# Source Catalog Tooling

This folder should host scripts that:

- ingest raw archive metadata
- normalize package records
- tag trust tiers
- produce implementation task lists from confirmed signals

The catalog is a first-class system, not a side note.

